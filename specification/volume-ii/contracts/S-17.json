{
  "slice": "S-17",
  "title": "Historical Comparison",
  "governing_sources": [
    "specification/volume-i/CAPABILITY_MODEL.md#cap-019-historical-comparison",
    "specification/volume-i/WORKFLOW_SPECIFICATIONS.md#wf-012-compare-historical-results",
    "specification/volume-i/PRODUCT_RULES.md PRULE-032 (owned here on MTX-083); PRULE-030 (row MTX-081, owned by S-16 Reporting) and PRULE-031 (row MTX-082, owned by S-16) are consumed, not redefined",
    "specification/volume-i/SCORE_EVIDENCE_MODEL.md#reassessment-result-and-historical-comparison (the comparison compatibility predicate, the eight fixed-order mismatch codes, the rebase contract and the insufficient-history contract)",
    "specification/volume-i/SCORE_EVIDENCE_MODEL.md#score-visibility-and-redaction (the History column, classification ceilings and `redacted_field_codes` groups)",
    "specification/volume-i/SCORE_EVIDENCE_MODEL.md#current-score-projection (consumed read-only; the comparison never writes it and no rebase snapshot moves its pointers)",
    "specification/volume-i/WORKFLOW_SPECIFICATIONS.md#permission-baseline, #logical-command-envelope-and-replay, #logical-result-and-error-contract, #logical-event-envelope",
    "specification/volume-i/WORKFLOW_SPECIFICATIONS.md WF-015 low-cost operation enumeration (`history.view`); the Entitlement Decision and LowCostUsageRecord contract is owned by S-22 on MTX-040 and MTX-024 and is consumed here, not redefined",
    "specification/volume-i/ACCEPTANCE_AND_TEST_MAPPING.md AC-CAP-019, AC-WF-012, AC-PRULE-032",
    "specification/volume-i/OWNER_DECISION_REGISTER.md OD-002 (ratified, Option 1), OD-003 (ratified, Option 3), OD-009 (ratified, Option 2), OD-019 (ratified, `read-metering-v1`), OD-020 (ratified, Option 1), OD-024 (ratified, Option 2 - no domain event)",
    "specification/volume-ii/contracts/S-13.json (MTX-076, AC-PRULE-025 - the ScoreSnapshot creation, lineage and idempotency contract the rebase delegates to; consumed not redefined)",
    "specification/volume-ii/contracts/S-16.json (MTX-081, AC-PRULE-030 - the deterministic-structured-only dashboard/history response rule; consumed not redefined)",
    "specification/015 DATA_LIFECYCLE.md `product_history` retention class",
    "schemas/POSTGRESQL_SCHEMA.md `score_snapshots`, `score_contributions`, `current_score_projections`; the event-registry manifest exclusion of `ComparisonGenerated`"
  ],
  "od_024_position": "OD-024 is RATIFIED and its `Current Status` reads 'Ratified on 2026-07-17 ...; resolved by owner decision'; `Blocking Impact`: 'None. OD-024 is resolved; comparison reads are unblocked and emit no domain event.' Approved Option 2: `ComparisonGenerated` is REMOVED as a Volume I domain event. The WF-012 comparison read is audit-only and side-effect-free - it persists no comparison record, moves no current pointer and emits no domain event - and every outcome remains fully deterministic under SCORE_EVIDENCE_MODEL.md. WF-012's audit obligations are discharged ENTIRELY by Audit Evidence with correlation ID. This is settled baseline, not a withheld limb: comparison remains fully functional and reaches its outcome without the event. Volume II MUST NOT emit, suppress, deduplicate or meter the removed name, and schemas/POSTGRESQL_SCHEMA.md permanently excludes `ComparisonGenerated` from the executable event-registry manifest so no generic Event row may ever admit it. Reintroduction requires controlled Volume I change. This slice therefore contracts ZERO domain events and asserts their absence as a test obligation rather than leaving it implied.",
  "od_020_position": "OD-020 is RATIFIED; Approved Option 1 - explicit read rows are added for Organization home data, Project, Source, Crawl, Evaluation, Notification inbox and Export enumeration, and 'Deny-by-default is not accepted for customer-facing objects'. Its effect on this slice is a GRANT that was never in doubt: `history.read` is already one of the Permission Baseline's enumerated read actions, named in OD-020's own Why-The-Decision-Exists among the closed set of existing reads, so the comparison read has canonical read authority and needs none inferred. OD-020 additionally makes `project.read` canonical, which is what allows the comparison's Project-scoped route to resolve its target. Nothing in this slice reads a security or administrative object, so OD-020's remaining deny-by-default limb is not reached here.",
  "no_pending_decision_touches_this_slice": "S-17's three rows cite OD-002, OD-003, OD-009, OD-020 and OD-024. Each is ratified; none of the five pending decisions - OD-014, OD-023, OD-027, OD-031, OD-032 - appears on any S-17 row, and none of their withheld limbs is reachable from a historical comparison. SLICE_REGISTER.md records S-17's OD limbs as 'none'. This slice therefore withholds nothing and asserts no limb as pending.",
  "rebase_ownership_split": "The rebase spans two slices and this contract owns only one side of it. S-17 owns the rebase REQUEST as WF-012's Alternate Path defines it: the `score.rebase` authorization, the requirement of an exact target version for every one of the seven versioned dimensions available to BOTH retained input sets, the rejection of omission or ambiguity, and the rule that rebase can never overcome different normalized `scope_definition_hash` values. S-13 owns what the accepted request then creates: the ScoreSnapshot contract on row MTX-076 (AC-PRULE-025) supplies the `historical_rebase` `creation_reason`, the immutability and idempotency tuple, the `prior_score_snapshot_id` lineage, the per-Project serialization, the two-snapshot creation order (earlier Evaluation by `created_at_utc`, then Evaluation ID, created first) and the rule that a rebase snapshot is permanently noncurrent and advances no Current Score Projection pointer. This row consumes those and redefines none of them.",
  "status_token_divergence": "Volume I spells the compatible outcome two ways. CAP-019's Outputs field - the authoritative enumeration of this capability's outputs - names `comparable`, `not_comparable`, `insufficient_history` and `comparison_unavailable`, and the `QRY-008 HistoryComparison` DTO in API_CONTRACTS.md carries exactly `enum{comparable,not_comparable,insufficient_history,comparison_unavailable}`. OD-024's Ratified Behavior and WF-012's Domain Events line instead write `compatible` in the same list, and AC-CAP-019/AC-WF-012 use 'Compatible' adjectivally. This contract uses `comparable` as the status token because the two places that enumerate the value as a value agree on it, and records `compatible` as prose describing the same outcome. No third status exists and this row mints none.",
  "volume_ii_exposure_position": "Two Volume II table cells describe `QRY-008` as switched off, and both are stale Pass-A-era prose that this slice supersedes. APPLICATION_LAYER.md line 159 says the query is 'disabled by `UPSTREAM-V1-LOW-COST-METERING-005` and `UPSTREAM-V1-COMPARISON-EVENT-007`'; API_CONTRACTS.md line 593 marks the physical path 'deferred under' the same two tags. The evidence that they are stale is executable, not interpretive. (1) INDEX.md's canonical blocker registry records BOTH tags 'Retired under ADR-019'; only `UPSTREAM-V1-PROJECT-LIFECYCLE-003` (OD-014) and `UPSTREAM-V1-CREDENTIAL-ROTATION-TOKEN-009` (OD-023) are LIVE. (2) OD-024's `Blocking Impact` is affirmative rather than merely absent: 'None. OD-024 is resolved; comparison reads are unblocked and emit no domain event.' OD-019's is 'None.' (3) Decisively, the IMPLEMENTATION_MATRIX is generated and its `Blocker` column is computed by `scripts/build_volume_ii_matrix.py` from each cited decision's `Current Status` ALONE - a row is blocked if and only if one of its ODs is pending. MTX-019, MTX-037 and MTX-083 each carry `Blocker: None` and `Status: Pass B required`. Neither retired tag appears as a blocker on ANY of the 97 matrix rows; the only blockers present anywhere are the five pending decisions and their two live tags. The matrix is 'the controlling source for Pass B', so the controlling source says nothing is blocked here. (4) The INDEX prose reserves the word 'unreachable' for what is genuinely withheld - `UPSTREAM-V1-CREDENTIAL-ROTATION-TOKEN-009` says 'Rotation begin/completion remains unreachable' - and the `UPSTREAM-V1-COMPARISON-EVENT-007` section uses no such word. Reading those two cells as live would withhold behaviour the owner affirmatively unblocked, which is the OD-001 failure mode the overlay exists to prevent. THE BOUNDARY: 'deferred until the Volume II baseline' resolves TO this pass, but it is a DOCUMENT-OWNERSHIP statement, not a grant to a slice contract. The exposure is deferred to API_CONTRACTS.md and APPLICATION_LAYER.md - the documents that own transport and the query registry - and not to S-17.json, which is why this slice defines no route, exactly as every completed slice did (S-21's route field: 'Transport exposure is owned by API_CONTRACTS.md at the Volume II baseline, which already carries POST /api/v1/investigations ...; this row neither renames nor re-derives them'). Nothing needs inventing in any case: API_CONTRACTS.md ALREADY carries the project-scoped history-compare GET against `QRY-008` and `history.read`. The transport was authorized; only the deferral marker on it is stale. This slice therefore contracts the comparison read's full semantics as reachable, withholds nothing on those cells' authority, mints no route, and reports the stale cells for correction by the document that owns them.",
  "od_019_declaration_gap": "Clearing the stale deferral marker is necessary but NOT sufficient to make the comparison route reachable, and this slice reports the reason rather than closing it. OD-019's Ratified Behavior requires that 'Every metered read route carries a static declaration of exactly one of the five low-cost operations; a read route with no declaration resolves `operation_unknown` and returns Block with `contact_support`, so an undeclared metered route is unreachable rather than silently unmetered.' The comparison read is a metered low-cost read: WF-015 enumerates `history.view` among exactly five low-cost operations. But API_CONTRACTS.md's route table has columns `Route | Query | Permission | Enablement` and NO operation-declaration column, and no route in Volume II carries such a declaration; the `operation_unknown` reason code and `contact_support` recovery action already exist in `EntitlementNoticeDTO`, so the Block-on-undeclared path is modelled while the declaration that avoids it is not. This gap is not S-17's to close on three counts: it spans all five low-cost operations across every metered read route rather than this one; the metering contract is WF-015 and CAP-024, owned by S-22 on MTX-040 and MTX-024; and inventing a declaration mechanism would mint a transport contract no one authorized. Note also that the five low-cost operation strings are NOT permissions - `history.view` does not exist in the Permission Baseline, whose read is `history.read` - so the declaration cannot be inferred from the route's permission cell. S-17 names `history.view` as the operation this read declares and stops there.",
  "recorded_divergences": "Four follow-on corrections are outside S-17's authority and are recorded rather than made. (1) API_CONTRACTS.md line 593 still marks the `QRY-008` route 'deferred under `UPSTREAM-V1-LOW-COST-METERING-005` and `UPSTREAM-V1-COMPARISON-EVENT-007`', and APPLICATION_LAYER.md line 159 still marks the query 'disabled by' the same two blockers, while INDEX.md records BOTH as 'Retired under ADR-019' and the register gives OD-019 and OD-024 `Blocking Impact` of None. (2) FRONTEND_ARCHITECTURE.md line 125 and the `WEB-020` row likewise carry both retired blockers as live. (3) APPLICATION_LAYER.md line 159 names the `QRY-008` read source as 'promoted snapshots and comparison projection', but OD-024 Option 2 persists no comparison record and no comparison table exists in schemas/POSTGRESQL_SCHEMA.md; the phrase describes an artifact the ratified option removed. (4) Volume I's `redacted_field_codes` is the `redaction_codes` field in the `QRY-008` and `HistoryItemDTO` shapes; the semantic groups are identical and only the field name differs. Each is stated as a divergence; correcting the owning document is that document's own controlled change and not this slice's to make.",
  "authority_gap_projection_rebuild": "Volume I names an obligation here for which it supplies no implementation surface, and this slice reports the gap rather than closing it. WF-012's Failure Path returns `comparison_unavailable` for 'Missing projection data' and its Recovery Path offers 'retry projection generation', bounded to one initial attempt plus two retries at 1 and 5 minutes before terminal `comparison_unavailable` and escalation to support. Under OD-024 Option 2 NOTHING is persisted by the comparison read, so there is no comparison projection to be missing or to rebuild, and no comparison table, job or read model exists in schemas/POSTGRESQL_SCHEMA.md or BACKGROUND_PROCESSING.md. OD-024's own Operational Implications flagged exactly this: 'every option must reconcile WF-012's \"Missing projection data\" and \"Projection rebuild\" language with the chosen model or clarify that it refers to the Current Score Projection' - and the Ratified Behavior does not perform that reconciliation. The Current Score Projection is the only projection Volume I defines that can be unavailable, but historical comparison selects two immutable promoted ScoreSnapshots by ID and does not read it for either side, so it cannot be the referent without a controlled change that says so. This row therefore contracts the parts that ARE fixed - `comparison_unavailable` is a reachable terminal outcome carrying a correlation ID, it synthesizes no values, and the retry bound is exactly one attempt plus two retries at 1 and 5 minutes then terminal plus support escalation - and invents no comparison projection entity, table, job or rebuild command to sit behind them. The slice still reaches its outcome: `comparable`, `not_comparable` and `insufficient_history` are each fully determined without any projection artifact.",
  "rows": {
    "MTX-037": {
      "contract_owner": "specification/volume-ii/APPLICATION_LAYER.md#wf-012-compare-historical-results",
      "interface_type": "One authorized read query, `QRY-008 HistoryComparison`, plus one state-changing rebase request that the Alternate Path authorizes and delegates. WF-012's Trigger is a user opening the historical comparison view and its State Transitions line records no domain entity transition, so the comparison itself is a read that issues no command. The rebase is the single exception: an explicitly requested OrganizationAdmin operation that creates two immutable noncurrent ScoreSnapshots before comparison. This row owns the rebase request - authorization, the exact-target-version predicate and the rejection outcomes - and delegates snapshot creation to MTX-076 in S-13.",
      "route": "Not applicable - Volume I defines the actor, permission, preconditions, paths and outcomes but no HTTP path, method or transport. Transport exposure is owned by API_CONTRACTS.md at the Volume II baseline, which already carries the project-scoped history-compare GET against `QRY-008 HistoryComparison` and `history.read`; this row neither renames nor re-derives it. That mapping is still marked deferred under `UPSTREAM-V1-LOW-COST-METERING-005` and `UPSTREAM-V1-COMPARISON-EVENT-007`, and APPLICATION_LAYER.md's query registry still marks the query disabled by the same two tags. Both cells are STALE and this row does not honour them: INDEX.md's canonical registry records both tags 'Retired under ADR-019', OD-024's `Blocking Impact` reads 'None. OD-024 is resolved; comparison reads are unblocked and emit no domain event', OD-019's reads 'None', and the generated matrix - whose `Blocker` column is computed from each decision's `Current Status` alone - carries `Blocker: None` on this row and names neither tag as a blocker on any row in the corpus. Nothing is withheld on their authority; see `volume_ii_exposure_position`. Correcting the two cells is the owning documents' controlled change and not this row's, and the route the comparison declares as its metered operation is `history.view` - a declaration API_CONTRACTS.md's route table has no column for, per `od_019_declaration_gap`. Volume I names no rebase route at all and this row mints none.",
      "request_schema": "Comparison: exactly `earlier_score_snapshot_id: uuid` and `later_score_snapshot_id: uuid`, or BOTH omitted to request the canonical latest compatible pair. Exactly one supplied ID, the two IDs equal, or any other parameter is validation-invalid per the `QRY-008` query contract. Both snapshots must be promoted and belong to the addressed Project in the addressed Organization. Rebase: an exact target version for every one of the seven versioned dimensions - score policy, confidence policy, eligibility policy, fingerprint, check catalog, scope policy and pillar applicability - each available to both retained input sets, plus the two target Evaluation identities. Omission or ambiguity of any target version rejects the request. Normalized `scope_definition_hash` is not a rebase parameter: it is a property of the retained input sets and no request field can align two different values.",
      "response_schema": "The `QRY-008 HistoryComparison` shape: `type: enum{history_comparison}`; `id: sha256` over the ordered selected snapshot IDs; and attributes exactly `status: enum{comparable,not_comparable,insufficient_history,comparison_unavailable}`, `earlier: nullable object<HistoryItemDTO>`, `later: nullable object<HistoryItemDTO>`, `available_run_count: uint`, `overall_delta: nullable decimal`, `pillar_deltas: array<object<PillarDeltaDTO>>[0..7]` without duplicate `pillar`, `mismatch_codes: array<enum{...}>[0..8]`, `next_action_code: nullable enum{complete_initial_evaluation,complete_next_evaluation,select_compatible_pair,request_rebase,retry_projection}`, and `redaction_codes: array<ascii[1..120]>[0..*]`. `overall_delta` and `pillar_deltas` are nonnull/nonempty ONLY for `comparable`; `insufficient_history` returns null selected items and an exact `available_run_count` of 0 or 1. Each `PillarDeltaDTO` is exactly `pillar: PillarId`, `earlier_score`, `later_score` and `delta` in inclusive range `-100.0..100.0`. No narrative field, narrative placeholder, presentation-region hint, prompt, model, provider field or provider-call status exists in this shape.",
      "controller": "Not applicable - no HTTP adapter is defined by Volume I; see route. The Volume II physical adapter and presenter are owned by API_CONTRACTS.md and FRONTEND_ARCHITECTURE.md, whose `WEB-020` history screen maps this query. Per the registered query budgets, presenters and serializers execute zero SQL.",
      "command": "The comparison issues NO command: WF-012 declares no domain entity transition and is triggered by a safe read, which is precisely the property OD-024 Option 2 preserves. The rebase is the one command WF-012's Alternate Path names, and Volume I fixes its actor, permission, predicate and rejection outcomes without naming a command identifier or an owning command namespace. This row therefore contracts the rebase request contract and does NOT mint a `Workflows::Wf012::` command name for it; the snapshots it creates are the ScoreSnapshot contract's, owned by MTX-076 in S-13 under the `historical_rebase` creation reason.",
      "command_input": "For the rebase only, the Logical Command Envelope: `command_id`, `idempotency_key`, `command_type`, `schema_version`, the human `actor_id` of the requesting OrganizationAdmin, the nonnull `organization_id`, the nonnull `project_id` resolving to that Organization, the two target Evaluation identities, the seven exact target versions, `requested_at_utc`, `correlation_id`, `causation_id` and one schema-valid `command_payload`. Expiry and race decisions use the server-assigned `authorization_check_at_utc`, never the client instant. The comparison read carries no envelope: client-supplied idempotency keys remain prohibited on a read, and under `read-metering-v1` its Decision ID is server-minted deterministically from Organization, human Account or service identity, declared operation, resolved target identity and state version, and counter window.",
      "command_output": "For the rebase, one Logical Result: `outcome=success` carrying the two created immutable noncurrent ScoreSnapshot identities, or `outcome=failure` with `error_class`, `error_code`, one contract-defined `reason_code`, `severity`, `retryable` and one `recovery_action`. A successful rebase returns snapshots, not a comparison: the comparison is then a separate read against the two new snapshot IDs. Neither rebase snapshot advances a Current Score Projection pointer, and a rejected rebase creates no snapshot at all.",
      "actor": "Per CAP-019 and WF-012: an Organization Administrator, Marketing Operator, Technical Implementer, read-only Executive Buyer, or a SecurityOperator in authorized incident or adjudication scope, each holding Organization-scoped `history.read`. The Permission Baseline allows `history.read` to OrganizationAdmin, MarketingOperator, TechnicalImplementer and the Read-Only Executive Buyer, restricts SecurityOperator to 'authorized incident/adjudication scope only', DENIES it to BillingOperator, and admits a Service Identity 'workflow-specific only'. The Executive Buyer is a product persona, not a foundation role: it uses a read-only MarketingOperator assignment, and the Score Visibility table gives that persona History access only through `internal`. Rebase narrows to exactly one actor: an OrganizationAdmin holding `score.rebase`, which the Permission Baseline denies to every other role and admits only as 'scoring service executes authorized request only'.",
      "organization_scope": "Exactly one Organization owns the addressed Project, both selected ScoreSnapshots, both Evaluations and every Score Contribution the comparison reads. `organization_id` is the F1 tenant identity and every nonnull Project, target, actor Account, service scope and policy reference resolves to it before authorization. A Project-scoped request with null or cross-Organization `project_id` is `F1-AUTH-403 / tenant_mismatch` with no side effect. A comparison never spans Organizations or Projects: two snapshots from different Projects are not a mismatch to be reported as a code, they are an unresolvable target.",
      "aggregate": "There is no Comparison aggregate and this row creates none - OD-024 records that none exists in the domain model, state model or schema, and that this is exactly why neither the `decision` nor the `projection` event profile could be satisfied. The comparison is a pure read over two immutable ScoreSnapshot roots and their immutable Score Contributions. ScoreSnapshot is the root that matters here and it is owned by MTX-076 in S-13; this row reads it and never writes it.",
      "aggregate_boundary": "The boundary that matters is what the comparison may NOT do. It writes no comparison record, moves no current pointer, emits no domain event, and mutates no ScoreSnapshot, Score Contribution, Issue, Case, Evidence record or Current Score Projection. It reads state-at-snapshot data only: each ScoreSnapshot's immutable Contributions carry the captured Issue state version, lifecycle, publication and adjudication values, Case references, Evidence IDs and Validation Decision IDs and statuses, and a later Issue or Evidence decision does not rewrite them. The rebase crosses out of this boundary deliberately and only through the ScoreSnapshot contract in S-13, under `score.rebase`, creating permanently noncurrent snapshots; even then it never mutates the originals and never advances a pointer.",
      "value_objects": "ScoreSnapshotId, EvaluationId, ProjectId, ComparisonStatus restricted to `comparable`, `not_comparable`, `insufficient_history`, `comparison_unavailable`, MismatchCode restricted to the eight fixed-order codes, NextActionCode restricted to `complete_initial_evaluation`, `complete_next_evaluation`, `select_compatible_pair`, `request_rebase`, `retry_projection`, AvailableRunCount, PillarId restricted to the seven canonical pillars `technical_integrity`, `search_presence`, `ai_presence`, `authority_signals`, `trust_signals`, `content_quality`, `local_presence`, ScoreDecimal as a one-decimal nonexponent string in `0.0..100.0`, PillarDelta in inclusive range `-100.0..100.0`, ScopeDefinitionSha256, ScopePolicyVersionSetSha256, ScorePolicyVersion, ConfidencePolicyVersion, EligibilityPolicyVersion, FingerprintVersion, CheckCatalogVersion, PillarApplicabilityVersion, RedactedFieldCode, CorrelationId.",
      "domain_service": "Not applicable - WF-012 names no domain service. Compatibility is a pure equality predicate over eight frozen dimensions of two snapshots; the mismatch code list is a pure ordered projection of which of those equalities failed; each delta is pure subtraction over two frozen scores; and `available_run_count` is a pure count of completed Evaluations with promoted ScoreSnapshots. None is a collaborator with its own lifecycle. The scoring service that executes an authorized rebase is named by the Permission Baseline and belongs to the ScoreSnapshot contract in S-13, not to this read.",
      "repository": "`ScoreSnapshotRepository` and `ScoreContributionRepository`, both read-only at this row, resolving only promoted snapshots of the addressed Project; `EvaluationRepository` read-only for the completed-Evaluation count that determines `insufficient_history`. Unavailable and nonpromoted snapshots never enter the history collection. No repository at this row performs a write.",
      "persistence_model": "Canonical `score_snapshots` (`T-IMM`, `LINEAGE`) and `score_contributions` (`T-IMM`, `LINEAGE`) per schemas/POSTGRESQL_SCHEMA.md, read only. `score_snapshots` carries the Evaluation, Issue Set, Catalog, applicability and policy IDs and hashes, the prior Snapshot ID, calculation status and completeness, the ordered unavailable reasons, the input and output semantic hashes and the immutable eligibility class, with no mutable or current flag and unique `(project_id,sequence)`. `score_contributions` is unique `(score_snapshot_id,issue_id)` and carries the frozen Issue state and version. Both are `T-IMM` tenant tables with `ENABLE ROW LEVEL SECURITY` and `FORCE ROW LEVEL SECURITY`. `current_score_projections` (`T-MUT`) is NOT read for either side of the comparison and is never written by it. No comparison table exists and this row creates none.",
      "migration": "Not applicable at this row - the comparison is a read over existing immutable tables, adds no column, index or constraint, and persists nothing. `ComparisonGenerated` is permanently excluded from the executable event-registry manifest, so no migration may reintroduce the name without controlled Volume I change.",
      "transaction_boundary": "One authorized read transaction that establishes proved tenant context, resolves both promoted snapshots and their frozen Contributions, evaluates the compatibility predicate, computes deltas only when compatible, applies current field redaction and returns. It performs no write and holds no lock beyond the read. Per the registered budgets it sets `statement_timeout=450ms` and `lock_timeout=250ms`, spends at most 9 application SQL statements and materializes at most 500 rows. The rebase is a separate transaction owned by the ScoreSnapshot contract in S-13, serialized per Project, creating both noncurrent snapshots with the earlier Evaluation by `created_at_utc` then Evaluation ID first; the comparison read that follows is again its own read transaction. The `read-metering-v1` LowCostUsageRecord is appended at the durable response checkpoint under WF-015's contract, owned by S-22.",
      "concurrency": "The comparison reads only immutable rows, so there is no write race to lose: both ScoreSnapshots and every Score Contribution are `T-IMM` and cannot change under the read, which is what makes a repeated comparison of the same ordered pair return the same status, codes and deltas forever. A snapshot promoted concurrently with the read changes `available_run_count` and the canonical latest compatible pair for a LATER request; it never mutates a selected snapshot or retroactively alters a returned delta. A concurrent Issue adjudication, dispute or Evidence validation change marks the Current Score Projection unavailable and may suppress Recommendation Artifacts, but it rewrites no historical Contribution and therefore cannot change a comparison already determined from frozen state. The rebase serializes per Project under the ScoreSnapshot contract in S-13; two concurrent identical rebase requests resolve through that contract's idempotency tuple and cannot mint four snapshots.",
      "idempotency": "The comparison is a read and repeats no product side effect by construction, which is the property OD-024 Option 2 exists to preserve: no event to double-emit on re-open, Turbo Frame fetch or prefetch, and no record to duplicate. Its response identity is the `sha256` over the ordered selected snapshot IDs, which is stable because both snapshots are immutable. Client-supplied idempotency keys remain prohibited on the read. Metering idempotency is `read-metering-v1`'s, not this row's: exactly one LowCostUsageRecord is appended per accepted top-level document read reaching the durable response checkpoint, keyed by a server-minted Decision ID over Organization, actor or service identity, declared operation, resolved target identity and state version, and counter window; a repeated tuple within the window replays the stored record without another increment or event, and Turbo Frames of a declared root carry the root Decision ID and append no second record. The rebase is idempotent on the ScoreSnapshot idempotency tuple `(evaluation_id, score_policy_version, confidence_policy_version, eligibility_policy_version, scope_snapshot_id, input_set_hash, creation_reason)` owned by MTX-076: exact replay returns the existing snapshots and creates no second pair, and reuse with a different canonical request hash is `idempotency_conflict`.",
      "background_job": "Not applicable - WF-012 defines no background job. The comparison is a synchronous authorized read and neither enqueues nor awaits work. Volume I's 'retry projection generation' Recovery Path implies a rebuild whose artifact Volume I never names, and under OD-024 Option 2 no comparison projection is persisted for a job to rebuild; that gap is recorded in `authority_gap_projection_rebuild` and no job is invented to close it. The rebase's snapshot creation is a synchronous authorized command in S-13, not a queued job.",
      "queue": "Not applicable - no background job is defined at this row, so no queue class is claimed. See background_job.",
      "retry_policy": "The comparison has no automatic retry: `comparable`, `not_comparable` and `insufficient_history` are terminal deterministic outcomes reached in one read, and a `not_comparable` result is a correct answer rather than a transient failure to retry. WF-012's Recovery Path is operator-driven and exhaustive: select a compatible pair, request an authorized rebase, or retry projection generation. Only that third limb is bounded and time-fixed: projection rebuild is exactly one initial attempt plus two retries at 1 and 5 minutes, after which the result is terminal `comparison_unavailable` and escalates to support. This row contracts that bound exactly as stated and names no rebuild artifact behind it; see `authority_gap_projection_rebuild`. A rejected rebase is never retried implicitly under different versions - a new rebase is a new explicitly requested command with its own exact target versions.",
      "terminal_failure": "Per the Failure Path, exhaustively. Fewer than two completed Evaluations with promoted ScoreSnapshots returns `insufficient_history` with `available_run_count` of exactly 0 or 1 and `next_action_code=complete_initial_evaluation` for zero or `complete_next_evaluation` for one; it returns no empty and no synthetic delta. Mismatched required dimensions returns `not_comparable` with every applicable code in the fixed order and NO numeric score delta. Missing projection data returns `comparison_unavailable` with a correlation ID and synthesizes no values. A rebase missing or ambiguous on any target version, or naming a version unavailable to either retained input set, is rejected and creates no snapshot. Different normalized `scope_definition_hash` values remain `not_comparable` after any rebase. Absence of AI narrative NEVER enters a failure path: a structured response with no narrative is the complete successful baseline response.",
      "reconciliation": "Every comparison request reaches exactly one of four statuses and there is no fifth state in which a pair is quietly incomparable without a reason: `comparable` carries deltas, `not_comparable` carries every applicable mismatch code, `insufficient_history` carries the exact available count, and `comparison_unavailable` carries a correlation ID. That totality is what makes an unusable comparison auditable rather than approximated. `not_comparable` returns ALL applicable codes rather than the first one found, so a pair failing three dimensions reports three; a caller learns everything that must change, which is what makes `request_rebase` an actionable next step and what makes the permanent `scope_definition_mismatch` distinguishable from the rebaseable ones. History is never rewritten to reconcile a mismatch: state-at-snapshot data is reconstructed from immutable Contributions only.",
      "domain_events": "Exactly zero. Under OD-024, ratified Option 2, `ComparisonGenerated` is REMOVED as a Volume I domain event and the comparison read emits none - it persists no comparison record and moves no current pointer. This is settled baseline, not a withheld limb: every outcome remains fully deterministic and the workflow's audit obligations are discharged entirely by Audit Evidence with correlation ID, so no outcome depends on an event. `ComparisonGenerated` is permanently excluded from the executable event-registry manifest, so no generic Event row may admit it and no migration may reintroduce it without controlled Volume I change. Volume II MUST NOT emit, suppress, deduplicate or meter the removed name. The rebase's snapshot creation events, if any, belong to the ScoreSnapshot contract on MTX-076 in S-13 and are not emitted from this row. Comparison query latency and usage telemetry are technical telemetry and MUST NOT be emitted as domain events. AC-WF-012's word 'event' in its status/delta/reason/count/event list predates OD-024 and is satisfied here by asserting event absence.",
      "event_payload": "Not applicable - this row emits no domain event, so there is no envelope to populate. See domain_events.",
      "event_producer": "Not applicable - this row emits no domain event and no producer exists for a removed name. WF-012 is asserted absent from the event stream entirely. See domain_events.",
      "event_consumers": "Not applicable - no event is produced, so no consumer subscribes. The comparison result reaches its caller through the query response, not by reacting to an event, and the `notification-policy-v1` baseline route table lists no comparison trigger.",
      "serializer": "The `HistoryComparisonDTO` per API_CONTRACTS.md, whose shape is recursively closed: it contains no narrative, narrative placeholder, presentation-region hint, prompt, model, provider field or provider-call status, and MUST NOT reference `AiOrchestration`, expose a narrative field, reserve a narrative slot, enqueue narrative work or call a provider. Serialization applies current field redaction and emits stable `redaction_codes` for omitted fields only, never a value-derived placeholder. Serializers execute zero SQL. Physical transport serialization is owned by API_CONTRACTS.md at the Volume II baseline.",
      "authorization_entry_point": "The query handler, before any snapshot row is materialized and again before the response is serialized. Authorization resolves `history.read` for the addressed Organization and Project, and for a SecurityOperator additionally proves that an incident or adjudication scope is currently active - a standing SecurityOperator role is not history read authority. Field redaction is applied at serialization against the CURRENT actor and the strongest classification among each derived field's originating Evidence records, not against the actor or the classification frozen at snapshot time; a historical snapshot never carries forward the visibility of the actor who created it. The rebase reauthorizes separately at its own command: `score.rebase` is checked at the command, not inherited from the read that suggested it.",
      "permission_checks": "`history.read`, Organization-scoped. The Permission Baseline allows it to OrganizationAdmin, MarketingOperator, TechnicalImplementer and the Read-Only Executive Buyer, restricts SecurityOperator to authorized incident or adjudication scope only, DENIES it to BillingOperator, and admits a Service Identity workflow-specific only, with no interactive access per the Score Visibility table. It is not a protected grant and requires no approval or Support Session; a Support Session scopes an already-permitted action and is not itself a read grant and never widens a Permission Baseline cell. Undefined permission or scope is deny-by-default and Organization scope is mandatory for every allow. Rebase requires OrganizationAdmin `score.rebase`, which the Permission Baseline denies to MarketingOperator, TechnicalImplementer, SecurityOperator, BillingOperator and the Read-Only Executive Buyer alike; `history.read` never substitutes for it, and no rebase is inferred from a `not_comparable` result. Classification ceilings bind independently of the action permission: `confidential` for OrganizationAdmin and TechnicalImplementer, `internal` for MarketingOperator and the Executive Buyer persona, `restricted` for a SecurityOperator only inside active authorized scope. `evidence.restricted.read` raises only the granted OrganizationAdmin Evidence payload access and never broadens history field permissions.",
      "tenant_boundary": "One Organization, one Project, both snapshots, both Evaluations and every Contribution. Every `score_snapshots` and `score_contributions` row is a `T-IMM` tenant row under `ENABLE ROW LEVEL SECURITY` and `FORCE ROW LEVEL SECURITY`; application code cannot make tenant context authoritative by executing `SET`, `SET LOCAL`, `set_config` or a raw SQL wrapper. A human read enters through `f1_enter_human_context(session_token_sha256,correlation_id)`, which validates the authoritative Session row and current scope before returning a proved transaction-local identity; a SecurityOperator read in authorized scope enters through `f1_enter_scoped_security_context`, which requires exactly one of a Session token digest or an already worker-claimed Work Dispatch Binding ID, derives its actor from that restricted locator and then validates the exact active Support Session and Organization scope. Neither accepts a caller-supplied actor, Service Identity ID or scope. Cross-Organization fields never appear in a comparison response, and a snapshot ID from another Organization or Project does not resolve.",
      "error_contract": "Authorization: `F1-AUTH-403` for absent `history.read`, a SecurityOperator without active incident or adjudication scope, a BillingOperator, or a rebase without `score.rebase`; a Project-scoped request with null or cross-Organization `project_id` is `F1-AUTH-403 / tenant_mismatch`. An actor who cannot access the requested score object at all is denied with an auditable authorization error rather than served a fully redacted body; an actor who can access the object but not a field receives the field omitted with its stable code in `redacted_field_codes`. Validation: `F1-VALIDATION-400` for exactly one supplied snapshot ID, the two IDs equal, any other query parameter, a nonpromoted or unavailable snapshot ID, or a rebase omitting or ambiguating any of the seven target versions; `F1-VALIDATION-400 / command_schema_unsupported` for an unsupported major, a missing required field, a changed required-field type or a prohibited field on the rebase command, creating only the audited command outcome. Domain: `F1-DOMAIN-409` for a stale rebase `expected_state_version` or a race loss, and `F1-DOMAIN-409 / idempotency_conflict` for rebase reuse under a different canonical request hash. Note the distinction that carries this row: `not_comparable` and `insufficient_history` are SUCCESSFUL deterministic outcomes carrying reason codes, NOT errors - returning `F1-VALIDATION-400` for an incompatible pair would destroy the mismatch-code contract that tells a caller what to fix. `comparison_unavailable` is likewise a status, not an error envelope. Envelope derivation follows the fixed precedence - authentication, authorization or scope denial, a named dependency reason, a named attempt or deadline timeout reason, capacity, rate-limit, subsystem reasons, request-shape validation, domain state and concurrency, then data-integrity - and if no row selects unambiguously the workflow returns `F1-DATA-409 / contract_invariant_unmapped` and commits no side effect. No invented reason code is exposed outward.",
      "audit_record": "Per WF-012's Audit and Observability, exhaustively: actor and scope, the selected snapshot IDs, the compatibility inputs and result, the mismatch codes, the rebase snapshot IDs, the retry count, the redaction result, the latency and the correlation ID. Under OD-024 this audit trail is not merely supporting evidence - it is the ENTIRE discharge of the workflow's audit obligation, which is why the removed event costs the workflow nothing. The audit is written for every outcome including `not_comparable`, `insufficient_history` and `comparison_unavailable`, and every denied read and denied or rejected rebase is audited with its exact reason. Comparison audit records are `product_history`.",
      "observability": "The four statuses with their exact mismatch codes, available count, deltas and correlation ID carry the comparison's traceability; the audit trail is the observable record because no domain event exists. CAP-019's Observability Requirements name exactly comparison query latency and usage telemetry, and both are technical telemetry retained under `operational_telemetry`, asserted absent from the domain event stream. A `not_comparable` outcome is a first-class observable signal rather than an absence: the fixed-order codes name which dimensions diverged. The registered budget for `QRY-008 HistoryComparison` is at most 9 application SQL statements and at most 500 rows materialized over exactly two selected snapshots with no provider call, inside the read transaction's `statement_timeout=450ms`; the application-query portion targets at most 300ms p95 and render/serialization at most 100ms p95 within QA-REQ-005's provisional 500ms p95.",
      "retention": "ScoreSnapshots, Score Contributions and the comparison's audit records are `product_history`, retained 7 years on a cursor of 7 elapsed years after record creation or terminal transition, whichever is later, destroyed irreversibly only after reference-integrity proof, per 015 DATA_LIFECYCLE.md. The window is owned by that document and this row states no new one. Rebase snapshots enter the same class as ordinary immutable snapshots. Nothing else is persisted by the comparison, which is exactly why OD-024's Reversibility note holds that Option 2 is reversible by controlled change: no wrongly identified event ever entered 7-year retention. Comparison query latency and usage telemetry are `operational_telemetry` and not `product_history`.",
      "test_contracts": "TYP-SEC: `history.read` absent denies the request; a BillingOperator is denied; a SecurityOperator WITHOUT active incident or adjudication scope is denied and one WITH it is allowed, asserted as two fixtures rather than one. TYP-SEC: `history.read` is not accepted as rebase authority - a MarketingOperator, TechnicalImplementer, SecurityOperator, BillingOperator and Read-Only Executive Buyer are each denied `score.rebase`, and only an OrganizationAdmin holding it may rebase. TYP-DATA: a comparison of two compatible promoted snapshots returns `comparable` with exact deterministic structured deltas - `overall_delta` and one `PillarDeltaDTO` per applicable pillar without duplicate `pillar`, each delta in `-100.0..100.0` and equal to later minus earlier. TYP-DATA: every mismatch combination returns `not_comparable`, ALL applicable codes in the exact fixed order `score_policy_mismatch`, `confidence_policy_mismatch`, `eligibility_policy_mismatch`, `fingerprint_version_mismatch`, `check_catalog_version_mismatch`, `scope_policy_version_set_mismatch`, `scope_definition_mismatch`, `pillar_applicability_mismatch`, and NO numeric score delta - asserted for each single-dimension mismatch, for multi-dimension combinations, and for all eight at once, with order asserted rather than set membership. TYP-DATA: `insufficient_history` with zero completed Evaluations returns `available_run_count=0` and `next_action_code=complete_initial_evaluation`; with exactly one returns `available_run_count=1` and `complete_next_evaluation`; neither returns an empty or synthetic delta and both return null selected items. TYP-DATA: the zero/one/two boundary is asserted with one run either side, and an unavailable or nonpromoted snapshot never enters the history collection or raises the count. TYP-DATA: an authorized all-version rebase creates two ordered immutable noncurrent snapshots and comparison then proceeds; the earlier Evaluation by `created_at_utc` then Evaluation ID is created first; neither snapshot advances a Current Score Projection pointer and neither mutates an original. TYP-DATA: a rebase omitting any one of the seven target versions, ambiguous on any one, or naming a version unavailable to either retained input set is rejected and creates NO snapshot - asserted once per dimension. TYP-DATA: different normalized `scope_definition_hash` values remain `not_comparable` AFTER an otherwise complete and authorized rebase; rebase can never overcome them, asserted as a permanent outcome rather than a retryable one. TYP-DATA: state-at-snapshot only - a later Issue adjudication, dispute, withdrawal or Evidence validation change does NOT rewrite a historical comparison; the same ordered pair returns byte-identical status, codes and deltas before and after, while the Current Score Projection independently becomes unavailable with `invalid_evidence` or `issue_set_incomplete`. TYP-DATA: no synthesis - missing projection data returns `comparison_unavailable` with a correlation ID and fabricates no score, delta or count. TYP-DATA: the bounded rebuild is exactly one initial attempt plus two retries at 1 and 5 minutes, then terminal `comparison_unavailable` plus support escalation; a third retry is asserted absent and the 1- and 5-minute instants are asserted exactly. TYP-SEC: redaction - each role receives exactly its History-column result, with fields above its ceiling omitted and their stable codes returned in `redaction_codes`; an actor who cannot access the score object at all is denied with an auditable authorization error rather than served a redacted body; no value-derived placeholder is ever returned. TYP-SEC: the Executive Buyer persona reads History through `internal` only and is denied Contribution detail and Evidence entirely. TYP-SEC: the strongest classification among a derived field's originating Evidence records governs it, and no record, actor, policy or service may lower it. TYP-SEC: identical redaction outcomes across UI read model, logical API result, export, Notification and support view for the same fixture; cross-Organization fields never appear. TYP-SEC: redaction is applied against the CURRENT actor at read time, not the actor frozen at snapshot creation. TYP-OBS: the audit record contains actor, scope, selected snapshot IDs, compatibility inputs and result, mismatch codes, rebase snapshot IDs, retry count, redaction result, latency and correlation ID, for EVERY outcome including denials - and is asserted to be the complete discharge of the workflow's audit obligation with no event present. TYP-OBS: no domain event is emitted on ANY comparison path - asserted for `comparable`, `not_comparable`, `insufficient_history` and `comparison_unavailable`, and asserted for re-open, reload, Turbo Frame fetch, prefetch and repeated selection of the same pair. TYP-OBS: `ComparisonGenerated` is asserted absent from the event stream and rejected by the executable event-registry manifest. TYP-OBS: comparison query latency and usage telemetry exist as telemetry and are asserted absent from the domain event stream. TYP-AI: provider-available, provider-unavailable, feature-flag, tenant-setting, UI-layout and implementation-choice fixtures each create NO AI narrative, no narrative field, no narrative placeholder, no reserved presentation region and NO AI-provider call, and each returns the same complete successful structured response; narrative absence is asserted NOT to be an error, degraded state, incomplete response or fallback. TYP-AI: no hidden enablement path exists - no feature flag, provider availability, model capability, tenant setting, UI layout or implementation choice can enable narrative, asserted as unreachable rather than merely disabled. TYP-AI: deterministic human-authored labels, already-defined templates and existing deterministic score, trend, Issue, Evidence and Recommendation explanations remain unchanged and present. TYP-INT: the read is side-effect-free - after any comparison, `score_snapshots`, `score_contributions` and `current_score_projections` are byte-identical to their pre-read state and no comparison row exists anywhere. TYP-INT: the query contract - exactly two snapshot IDs, or both omitted for the canonical latest compatible pair; exactly one supplied ID, the two equal, or any other parameter is validation-invalid. TYP-INT: replay - the same ordered pair returns the same `id` sha256 and the same body; a client-supplied idempotency key on the read is rejected. TYP-INT: rebase replay under the ScoreSnapshot idempotency tuple returns the existing pair and mints no second pair; reuse with a different canonical request hash is `idempotency_conflict`. TYP-INT: the registered budget holds - at most 9 application SQL statements and at most 500 rows for `QRY-008`, with zero SQL executed in presenters and serializers and no provider call on any path.",
      "rollout": "S-17 depends on S-16 Reporting and gates nothing; SLICE_REGISTER.md records its OD limbs as 'none' and this contract withholds nothing. This row owns the WF-012 read and the rebase request. The ScoreSnapshot creation, lineage and idempotency the rebase delegates to are MTX-076 (AC-PRULE-025, S-13 Scoring); the deterministic-structured-only dashboard/history response rule is MTX-081 (AC-PRULE-030, S-16 Reporting); both are consumed here and neither is redefined. The `history.view` metering unit, its Entitlement Decision and its LowCostUsageRecord are WF-015's and are owned by S-22. Four Volume II divergences are recorded rather than corrected and one authority gap is reported rather than closed; see `recorded_divergences` and `authority_gap_projection_rebuild`. No predecessor behaviour exists."
    },
    "MTX-019": {
      "contract_owner": "specification/volume-ii/APPLICATION_LAYER.md#cap-019-historical-comparison",
      "interface_type": "Capability-level obligation discharged by the WF-012 contract on MTX-037 plus the PRULE-032 compatibility and rebase contract on MTX-083. CAP-019 defines no interface of its own; it fixes the actor set, the four outputs, the success and failure conditions, and one prohibition this row owns outright - the AI-narrative exclusion, which is capability-level product authority rather than a workflow step.",
      "route": "Not applicable - Volume I defines no HTTP path for this capability; transport exposure is owned by API_CONTRACTS.md at the Volume II baseline. See MTX-037's route, which this row does not re-derive.",
      "request_schema": "As CAP-019 Inputs: ScoreSnapshot history, Issue supersession state and Recommendation outcomes, addressed by the `QRY-008` query contract on MTX-037. This row adds no input of its own and, in particular, adds no narrative, tone, audience, prompt, model, provider or presentation-region parameter - there is no input that could request a narrative because no narrative exists to request.",
      "response_schema": "As CAP-019 Outputs: a `comparable`, `not_comparable`, `insufficient_history` or `comparison_unavailable` result with exact reason codes and the permitted next action, serialized by the `HistoryComparisonDTO` on MTX-037. The shape is recursively closed and contains no narrative field, no narrative placeholder, no presentation-region hint, no prompt, model or provider field and no provider-call status. Absence of narrative is the complete successful response.",
      "controller": "Not applicable - no HTTP adapter is defined by Volume I; see MTX-037's controller and route.",
      "command": "The capability issues no command of its own. Its numeric comparison is the read on MTX-037; its rebase limb is the request on MTX-037 delegating snapshot creation to MTX-076 in S-13. CAP-019's Non-goal names predictive forecasting commitments as outside baseline scope, so no forecast, projection-forward or trend-extrapolation command exists here.",
      "command_input": "Not applicable - this capability contributes no command; see command. The rebase envelope is contracted on MTX-037.",
      "command_output": "Not applicable - this capability contributes no command; see command. The rebase result is contracted on MTX-037.",
      "actor": "Per CAP-019: an Organization Administrator, Marketing Operator, Technical Implementer, read-only Executive Buyer, or authorized SecurityOperator scope, each holding `history.read`. The Preconditions are exactly: the Project exists and the actor holds `history.read`; zero or one completed Evaluation is a VALID insufficient-history query rather than an error; and numeric comparison requires two completed promoted snapshots. Rebase narrows to an OrganizationAdmin with `score.rebase`. The Executive Buyer is a product persona using a read-only MarketingOperator assignment, not a foundation role.",
      "organization_scope": "Historical data access is scoped by tenant and role per CAP-019's Security Implications. Exactly one Organization and one Project own every snapshot the capability reads; see MTX-037's organization_scope, which this row does not re-derive.",
      "aggregate": "No aggregate is introduced. CAP-019 reads immutable ScoreSnapshot roots and their Contributions, owned by MTX-076 in S-13. OD-024 records that no Comparison aggregate exists in the domain model, state model or schema, and this row creates none.",
      "aggregate_boundary": "The capability's boundary is defined by its Failure Condition, which names four things that MUST NOT occur: silent cross-version comparison, synthetic missing data, later-state rewrite of history, and unbounded projection rebuild. Each maps to a contracted guard - the compatibility predicate on MTX-083, the no-synthesis rule on MTX-037, the state-at-snapshot rule on MTX-037, and the one-attempt-plus-two-retries bound on MTX-037. The AI-narrative prohibition is a fifth boundary this row owns directly. Predictive forecasting is a Non-goal and is outside the boundary entirely.",
      "value_objects": "As MTX-037's value objects; this row introduces none of its own. The four statuses, eight mismatch codes, five next-action codes and seven PillarIds are enumerated there.",
      "domain_service": "Not applicable - CAP-019 names no domain service, and its AI Implications name none in the accepted baseline. There is deliberately no narrative service, no AI orchestration collaborator and no provider adapter reachable from this capability.",
      "repository": "As MTX-037's repositories, all read-only; this row adds none. No repository resolves a narrative, prompt, AIResponse or Citation record for this capability.",
      "persistence_model": "As MTX-037's persistence model - immutable `score_snapshots` and `score_contributions`, read only. CAP-019's Data Implications state only that historical snapshots are retained according to lifecycle policy, which is the `product_history` class; the capability introduces no table, and specifically no narrative, prompt or provider-response column.",
      "migration": "Not applicable at this row - the capability adds no schema object. See MTX-037's migration.",
      "transaction_boundary": "As MTX-037's transaction boundary - one authorized read transaction, with the rebase's snapshot creation in its own transaction owned by S-13. This row adds no transaction and, having no AI-provider call, has no external call to place inside or outside one.",
      "concurrency": "As MTX-037's concurrency - the capability reads immutable rows, so a repeated comparison of the same ordered pair is stable under any concurrent promotion, adjudication or validation change. This row adds no concurrency surface.",
      "idempotency": "As MTX-037's idempotency - the read repeats no product side effect, its response identity is the sha256 over the ordered selected snapshot IDs, and the rebase is idempotent on the ScoreSnapshot idempotency tuple owned by MTX-076. This row adds no idempotency key.",
      "background_job": "Not applicable - CAP-019 defines no background job and no narrative or provider work is ever enqueued. `QRY-008` MUST NOT enqueue narrative work. See MTX-037's background_job for the recorded projection-rebuild gap.",
      "queue": "Not applicable - no background job is defined at this row, so no queue class is claimed. See background_job.",
      "retry_policy": "As MTX-037's retry policy - the bounded projection rebuild is one initial attempt plus two retries at 1 and 5 minutes then terminal, which is precisely the guard against CAP-019's 'unbounded projection rebuild' Failure Condition. No narrative or provider retry exists because no narrative or provider call exists.",
      "terminal_failure": "Per CAP-019's Failure Condition, the capability fails if silent cross-version comparison, synthetic missing data, later-state rewrite of history, or unbounded projection rebuild occurs. Each is contracted as an asserted impossibility rather than an unlikely outcome. The Success Condition is equally exact: compatible pairs return reproducible attributable deltas; incompatible pairs return NO numeric score delta; fewer than two completed evaluations returns the EXACT available count. Narrative absence is never a failure.",
      "reconciliation": "As MTX-037's reconciliation - every request reaches exactly one of four statuses, each carrying its exact reason codes or count, with no fifth quiet state. This row adds no reconciliation loop.",
      "domain_events": "Exactly zero, per OD-024 as contracted on MTX-037. CAP-019 lists `ComparisonGenerated` in no enumeration and Volume I's foundation cross-context event list never named it, which is why ADR-015's stated minimum is not breached by its removal. This capability emits no domain event on any path.",
      "event_payload": "Not applicable - this capability emits no domain event. See domain_events.",
      "event_producer": "Not applicable - this capability emits no domain event and no producer exists. See domain_events.",
      "event_consumers": "Not applicable - no event is produced, so no consumer subscribes. See domain_events.",
      "serializer": "The `HistoryComparisonDTO` on MTX-037. This row owns one serializer-level prohibition outright: the DTO MUST NOT reference `AiOrchestration`, expose a narrative field, reserve a narrative slot, enqueue narrative work or call a provider, and it carries no prompt, model, provider field or provider-call status. A reserved-but-empty narrative region is itself a breach - CAP-019 prohibits reserving a presentation region for narrative, not merely populating one.",
      "authorization_entry_point": "As MTX-037's authorization entry point - the query handler, before any snapshot row is materialized and again at serialization for field redaction, with the rebase reauthorizing at its own command. This row adds no entry point.",
      "permission_checks": "`history.read`, Organization-scoped, exactly as contracted on MTX-037; rebase additionally requires OrganizationAdmin `score.rebase`. Under OD-020 `history.read` is already one of the Permission Baseline's enumerated read actions and needs no read authority inferred, and `project.read` is canonical for the Project-scoped target. No permission is inferred, derived from a write action, or widened by a Support Session.",
      "tenant_boundary": "As MTX-037's tenant boundary - one Organization, one Project, forced RLS on every `T-IMM` row, proved context through the reviewed entry functions, and no cross-Organization field in any response. CAP-019's Security Implications state exactly this: historical data access scoped by tenant and role.",
      "error_contract": "As MTX-037's error contract. This row adds one clause of its own: the absence of AI narrative NEVER produces an error, a degraded state, an incomplete response or a fallback, and no error code exists or may be minted for a missing, unavailable or failed narrative - because no narrative is ever attempted. `not_comparable` and `insufficient_history` remain successful outcomes with reason codes rather than error envelopes.",
      "audit_record": "As MTX-037's audit record, which under OD-024 is the complete discharge of the comparison's audit obligation. This row adds no audit field and specifically records no narrative decision, provider call or model selection, because none occurs.",
      "observability": "CAP-019's Observability Requirements name exactly two things: comparison query latency and usage telemetry. Both are technical telemetry retained under `operational_telemetry` and are asserted absent from the domain event stream; the registered `QRY-008` budget is 9 application SQL statements and 500 rows with no provider call. Zero provider calls and zero narrative paths are themselves observable assertions rather than assumptions, per the CAP-018/WF-012 zero-provider-call and zero-narrative-path assertions already registered in SECURITY_PERFORMANCE.md.",
      "retention": "Per CAP-019's Data Implications, historical snapshots are retained according to lifecycle policy: the `product_history` class at 7 years on a cursor of 7 elapsed years after record creation or terminal transition, whichever is later, per 015 DATA_LIFECYCLE.md. That document owns the window and this row states no new one. No narrative, prompt or provider response is retained because none is created.",
      "test_contracts": "TYP-AI: provider-available, provider-unavailable, feature-flag, tenant-setting, UI-layout and implementation-choice fixtures each produce NO AI narrative, no narrative field, no narrative placeholder, no reserved presentation region and NO AI-provider call, and each returns the identical complete successful structured response - asserted as six distinct fixtures, not one. TYP-AI: narrative absence is asserted NOT to be an error, degraded state, incomplete response or fallback; the response is complete and successful. TYP-AI: no hidden enablement - no feature flag, provider availability, model capability, tenant setting, UI layout or implementation choice can enable narrative; asserted unreachable rather than disabled, since a disabled path is one flag away from a prohibited one. TYP-AI: the DTO references no `AiOrchestration`, exposes no narrative field, reserves no narrative slot, enqueues no narrative work and makes no provider call; a reserved-but-empty region is asserted a breach. TYP-AI: deterministic human-authored labels, already-defined templates and existing deterministic score, trend, Issue, Evidence and Recommendation explanations remain permitted and unchanged - asserted present, so the prohibition is not over-applied into removing legitimate deterministic text. TYP-DATA: the Success Condition exactly - compatible pairs return reproducible attributable deltas, asserted by running the same pair twice and comparing byte-for-byte; incompatible pairs return no numeric score delta; fewer than two completed evaluations returns the exact available count of 0 or 1. TYP-DATA: the Failure Condition as four asserted impossibilities - no silent cross-version comparison (a mismatched pair NEVER returns a delta), no synthetic missing data, no later-state rewrite of history, no unbounded projection rebuild. TYP-DATA: zero or one completed Evaluation is a VALID query returning `insufficient_history`, asserted to succeed rather than raise. TYP-DATA: authorized all-version rebase creates ordered noncurrent snapshots, but different scope hashes remain noncomparable. TYP-SEC: each actor in the CAP-019 set - OrganizationAdmin, MarketingOperator, TechnicalImplementer, read-only Executive Buyer and authorized SecurityOperator scope - reads history exactly at its ceiling, and a BillingOperator is denied. TYP-SEC: historical data access is scoped by tenant and role; a snapshot from another Organization or Project does not resolve. TYP-SEC: predictive forecasting is asserted absent from baseline scope - no forecast, extrapolation or projection-forward field exists in any response.",
      "rollout": "S-17 depends on S-16 and gates nothing. This row is the capability-level obligation and is discharged by MTX-037 (WF-012) and MTX-083 (PRULE-032) within this slice, plus MTX-076 (S-13) and MTX-081 (S-16) consumed without redefinition. It withholds nothing: no pending owner decision touches CAP-019, whose only decisions are OD-020 and OD-024, both ratified. No predecessor behaviour exists."
    },
    "MTX-083": {
      "contract_owner": "specification/volume-ii/SECURITY_PERFORMANCE.md#prule-032-comparison-compatibility-and-rebase",
      "interface_type": "A cross-surface invariant over the comparison read, not an interface. PRULE-032 binds every reader of historical data regardless of surface or permission: comparison uses state-at-snapshot data only, returns no numeric delta unless EVERY required dimension matches, returns all fixed-order mismatch codes when they do not, and rebase requires all target versions and can never overcome different normalized scope hashes. It is enforced at the point the compatibility predicate is evaluated on MTX-037, and it is owned here because its failure mode - a silently comparable cross-version pair - is a correctness and integrity breach rather than a workflow step.",
      "route": "Not applicable - a rule constrains every surface that returns a comparison and mints no route of its own. The physical route is owned by API_CONTRACTS.md; see MTX-037's route.",
      "request_schema": "Not applicable - the rule constrains the response and the predicate, not the request shape. The one request-side clause it owns is the rebase's: an exact target version for every one of the seven versioned dimensions, each available to both retained input sets, with omission or ambiguity rejecting the request. The `QRY-008` query shape is contracted on MTX-037.",
      "response_schema": "The rule fixes exactly two response-side facts. First, `mismatch_codes` contains EVERY applicable code from the closed set of eight, in this exact fixed order: `score_policy_mismatch`, `confidence_policy_mismatch`, `eligibility_policy_mismatch`, `fingerprint_version_mismatch`, `check_catalog_version_mismatch`, `scope_policy_version_set_mismatch`, `scope_definition_mismatch`, `pillar_applicability_mismatch`. Second, a `not_comparable` result contains NO numeric score delta - `overall_delta` is null and `pillar_deltas` is empty. The `HistoryItemDTO` carries the eight comparable dimensions as `score_policy_version`, `confidence_policy_version`, `eligibility_policy_version`, `fingerprint_version`, `check_catalog_version`, `scope_policy_version_set_hash`, `scope_definition_hash` and `pillar_applicability_version`, which is what makes the predicate independently checkable from the response itself.",
      "controller": "Not applicable - the rule is enforced in the comparison predicate, not in an HTTP adapter; no controller is defined by Volume I. See MTX-037.",
      "command": "The rule contributes no command. It constrains the rebase command's admission - all seven target versions exact and available to both retained input sets - and constrains its reach: rebase can never make different normalized `scope_definition_hash` values comparable. The snapshots an admitted rebase creates are MTX-076's in S-13.",
      "command_input": "Not applicable - the rule adds no command envelope field. The rebase envelope is contracted on MTX-037.",
      "command_output": "Not applicable - the rule adds no command result field. The rebase result is contracted on MTX-037.",
      "actor": "The rule binds every actor with no exception: OrganizationAdmin, MarketingOperator, TechnicalImplementer, read-only Executive Buyer, authorized SecurityOperator scope and any tenant-scoped service identity are each subject to the identical compatibility predicate. No role sees a delta another role may not, because compatibility is a property of the two snapshots and not of the reader. `score.rebase` authorizes an OrganizationAdmin to CREATE compatible snapshots; it does not authorize reading a delta across incompatible ones.",
      "organization_scope": "Both snapshots resolve to one Organization and one Project before the predicate is evaluated; a cross-Organization pair is a tenant failure and never a mismatch code. See MTX-037's organization_scope.",
      "aggregate": "No aggregate - the rule is an invariant over two immutable ScoreSnapshot roots owned by MTX-076 in S-13. No Comparison aggregate exists in the domain model, state model or schema.",
      "aggregate_boundary": "The rule's boundary is what a rebase may reach. A rebase may create new immutable noncurrent snapshots under exact requested versions for the seven versioned dimensions; it may NOT mutate an original snapshot, advance a current pointer, or align a normalized `scope_definition_hash`. That last exclusion is the load-bearing one: seven dimensions are versioned policy that a rebase can re-run the calculation under, but the scope-definition hash is a normalized description of WHAT was measured, and two evaluations of different scopes are not the same measurement under a different policy. A rebase that could align it would silently redefine the subject rather than the method.",
      "value_objects": "MismatchCode restricted to the eight fixed-order codes; ScorePolicyVersion, ConfidencePolicyVersion, EligibilityPolicyVersion, FingerprintVersion, CheckCatalogVersion, ScopePolicyVersionSetSha256, ScopeDefinitionSha256 and PillarApplicabilityVersion as the eight compared dimensions; RebaseTargetVersionSet as the seven exact requestable versions. Note the asymmetry the rule fixes: eight dimensions are compared, seven are rebaseable, and `scope_definition_hash` is the one that is compared and never rebaseable.",
      "domain_service": "Not applicable - the rule names no domain service. Compatibility is a pure equality predicate over eight frozen values and the mismatch list is a pure ordered projection of its failures; neither is a collaborator with a lifecycle.",
      "repository": "As MTX-037's repositories, read-only. The rule adds none. The eight compared dimensions are frozen columns on `score_snapshots`, so the predicate needs no additional read.",
      "persistence_model": "The eight compared dimensions are frozen on the immutable `score_snapshots` row (`T-IMM`, `LINEAGE`) per schemas/POSTGRESQL_SCHEMA.md, which carries the Evaluation, Issue Set, Catalog, applicability and policy IDs and hashes. Their immutability is what makes the predicate reproducible: a comparison run today and in seven years over the same pair returns the same status and codes. The rule persists nothing itself.",
      "migration": "Not applicable at this row - the rule adds no schema object; the eight dimensions already exist as frozen columns. See MTX-037's migration.",
      "transaction_boundary": "The predicate is evaluated inside MTX-037's single authorized read transaction, over rows already materialized; it opens no transaction of its own and performs no write. An admitted rebase's snapshot creation is S-13's transaction.",
      "concurrency": "The predicate reads only immutable frozen columns, so it has no race: no concurrent write can change whether two given snapshots are compatible, which is why the answer is stable forever rather than merely at read time. A concurrently created rebase snapshot is a NEW pair to compare, never a mutation of an existing comparison.",
      "idempotency": "The predicate is a pure function of the two snapshots' eight frozen dimensions and therefore returns the identical status and identical ordered code list on every evaluation, with no key required. Rebase idempotency belongs to the ScoreSnapshot idempotency tuple on MTX-076: exact replay returns the existing pair, and reuse under a different canonical request hash is `idempotency_conflict`.",
      "background_job": "Not applicable - the rule defines no background job and no asynchronous work; the predicate is evaluated synchronously in the read.",
      "queue": "Not applicable - no background job is defined at this row, so no queue class is claimed. See background_job.",
      "retry_policy": "Not applicable - the predicate is deterministic and total over its inputs, so it has no transient failure to retry: `not_comparable` is a correct terminal answer and retrying it returns the same answer. A rejected rebase is not retried implicitly under inferred versions; a new rebase is a new explicit command with its own exact target versions.",
      "terminal_failure": "A pair failing any one of the eight equalities is `not_comparable` with every applicable code and no numeric delta - terminal for that pair unless a rebase creates a new one, and permanently terminal when `scope_definition_mismatch` is among the codes. A rebase omitting or ambiguating any of the seven target versions, or naming one unavailable to either retained input set, is rejected and creates no snapshot. A state failing the predicate but matching no defined code cannot occur, because the eight codes exhaustively cover the eight compared dimensions; the comparison mints no ninth code and infers no delta from a partial match.",
      "reconciliation": "The rule is total in both directions and that totality is the point. A `comparable` result means all eight equalities held, so a delta is attributable to real change rather than to a policy shift. A `not_comparable` result names EVERY dimension that diverged, not the first found, so a caller can tell in one response whether a rebase would help - seven codes are rebaseable, `scope_definition_mismatch` never is - rather than discovering a permanent barrier only after an authorized rebase completes. There is no third state in which a pair is compared under partial agreement.",
      "domain_events": "Exactly zero - the rule emits no event and, under OD-024, neither does the comparison it constrains. A mismatch is reported in the response and the audit record, never as a domain event. See MTX-037's domain_events.",
      "event_payload": "Not applicable - this row emits no domain event. See domain_events.",
      "event_producer": "Not applicable - this row emits no domain event and no producer exists. See domain_events.",
      "event_consumers": "Not applicable - no event is produced, so no consumer subscribes. See domain_events.",
      "serializer": "The `mismatch_codes` array in `HistoryComparisonDTO`, typed as a closed enum of exactly the eight codes with a `[0..8]` bound, serialized in the fixed order rather than in discovery order or sorted alphabetically. The eight compared dimensions are themselves serialized on each `HistoryItemDTO`, so a caller can independently verify the predicate against the response. Serialization emits no delta field value for `not_comparable`.",
      "authorization_entry_point": "The comparison predicate itself, evaluated on every comparison before any delta is computed and independently of the actor's permission - the rule is an invariant, not an authorization check, so no grant relaxes it. Rebase authorization is a separate check at the rebase command: `score.rebase`, verified there and never inherited from the read that produced the `not_comparable` result.",
      "permission_checks": "`score.rebase`, held by an OrganizationAdmin only. The Permission Baseline denies it to MarketingOperator, TechnicalImplementer, SecurityOperator, BillingOperator and the Read-Only Executive Buyer, and admits only 'scoring service executes authorized request only' - the service executes an authorized request and originates none. `history.read` never substitutes for `score.rebase`. Critically, NO permission - including `score.rebase` - authorizes a numeric delta across a mismatched pair or across different normalized scope hashes: the rule binds every actor, and an OrganizationAdmin holding every grant in the baseline still receives `not_comparable`.",
      "tenant_boundary": "Both snapshots resolve to one Organization and one Project before the predicate runs; forced RLS on `score_snapshots` makes a cross-Organization pair unresolvable rather than incomparable. A cross-tenant reference is `F1-AUTH-403 / tenant_mismatch`, never a mismatch code - the eight codes describe version divergence within a tenant and must never be used to report a tenant failure, which would leak the existence of another Organization's snapshot.",
      "error_contract": "The rule's outcomes are STATUSES, not error envelopes: `not_comparable` with its ordered codes is a successful deterministic response and returning `F1-VALIDATION-400` or `F1-DOMAIN-409` for an incompatible pair would destroy the mismatch-code contract that tells a caller what to fix. The rule's own error surface is the rebase's: `F1-VALIDATION-400` for an omitted or ambiguous target version or one unavailable to either retained input set, changing no state and creating no snapshot; `F1-AUTH-403` for a rebase without `score.rebase`. A cross-Organization snapshot reference is `F1-AUTH-403 / tenant_mismatch`. If the predicate could somehow select no code for a failed equality the workflow returns `F1-DATA-409 / contract_invariant_unmapped` and commits no side effect rather than returning a delta under partial agreement; no invented reason code is exposed outward.",
      "audit_record": "Per WF-012's Audit and Observability, the compatibility INPUTS and the result are audited alongside the mismatch codes and the rebase snapshot IDs - the inputs, not only the verdict, which is what makes a disputed `not_comparable` result reconstructible without re-reading the snapshots. Every rejected rebase is audited with its exact reason, including which target version was omitted, ambiguous or unavailable. Comparison audit records are `product_history`.",
      "observability": "The eight fixed-order codes are the observable signal: a `not_comparable` outcome names precisely which dimensions diverged and therefore whether a rebase is possible, and the order is itself part of the contract so that two implementations produce identical, diffable output. Rebase snapshot IDs and retry count are observable in the audit record. No domain event carries any of this, per OD-024. The predicate adds no SQL to the registered `QRY-008` budget of 9 statements and 500 rows because it reads columns already materialized for the response.",
      "retention": "The eight compared dimensions are frozen columns on `score_snapshots`, a `product_history` record retained 7 years on a cursor of 7 elapsed years after record creation or terminal transition, whichever is later, per 015 DATA_LIFECYCLE.md. Rebase snapshots enter the same class. The rule persists nothing of its own and states no new window; 015 DATA_LIFECYCLE.md owns it.",
      "test_contracts": "TYP-DATA: the compatibility predicate is exact and total - a pair is `comparable` ONLY when all eight of score policy, confidence policy, eligibility policy, fingerprint, check-catalog, scope-policy version set, normalized `scope_definition_hash` and pillar-applicability versions are identical; asserted with a fixture that matches on seven and diverges on one, once per dimension, each returning `not_comparable`. TYP-DATA: the code ORDER is fixed and asserted as a sequence, not a set - `score_policy_mismatch`, `confidence_policy_mismatch`, `eligibility_policy_mismatch`, `fingerprint_version_mismatch`, `check_catalog_version_mismatch`, `scope_policy_version_set_mismatch`, `scope_definition_mismatch`, `pillar_applicability_mismatch`; asserted for every combination including all eight diverging at once and for pairs whose divergent dimensions are discovered in a different physical column order. TYP-DATA: `not_comparable` returns NO numeric score delta - `overall_delta` null and `pillar_deltas` empty; asserted for every mismatch combination, so no partial-agreement delta ever leaks. TYP-DATA: state-at-snapshot only - the predicate reads the two snapshots' frozen dimensions and a later Issue, adjudication or Evidence decision changes neither the status nor the codes; asserted by comparing before and after such a decision. TYP-DATA: an authorized rebase supplying all seven exact target versions available to both retained input sets creates two immutable noncurrent snapshots and the resulting pair compares `comparable`. TYP-DATA: rebase rejection - omission of any one of the seven target versions, ambiguity on any one, and a version unavailable to either retained input set each reject the request and create NO snapshot; asserted once per dimension for each of the three rejection modes. TYP-DATA: rebase can NEVER overcome different normalized `scope_definition_hash` values - a fully specified, fully authorized, all-version rebase over a pair differing only in scope-definition hash still returns `not_comparable` with `scope_definition_mismatch`; asserted as permanent, with no next action that would resolve it. TYP-DATA: `scope_definition_mismatch` present in the codes means `request_rebase` is not returned as a next action, so the response never suggests an action that cannot succeed. TYP-DATA: a rebase never mutates an original snapshot and never advances a Current Score Projection pointer; both originals are asserted byte-identical after the rebase and the projection's pointers unchanged. TYP-INT: the predicate is pure and reproducible - the same pair evaluated twice returns identical status and identical ordered codes, and evaluation performs no write. TYP-INT: the eight compared dimensions are present on each `HistoryItemDTO` so the predicate is independently verifiable from the response alone. TYP-INT: `mismatch_codes` is a closed enum bounded `[0..8]` with no duplicate and no ninth code; an unmapped divergence is asserted impossible rather than serialized as a new code. TYP-INT: `mismatch_codes` is empty exactly when status is `comparable`. TYP-SEC: the rule binds every actor - an OrganizationAdmin holding `score.rebase` and every other baseline grant still receives `not_comparable` and no delta for a mismatched pair; no permission relaxes the predicate. TYP-SEC: `score.rebase` is denied to MarketingOperator, TechnicalImplementer, SecurityOperator, BillingOperator and the Read-Only Executive Buyer; `history.read` is not accepted as rebase authority. TYP-SEC: a cross-Organization or cross-Project snapshot pair returns `F1-AUTH-403 / tenant_mismatch` and is asserted NEVER to return a mismatch code, so no code discloses the existence of another tenant's snapshot.",
      "rollout": "S-17 depends on S-16 and gates nothing. This row owns the compatibility predicate, the eight fixed-order mismatch codes and the rebase admission and reach rules for the whole slice; MTX-037 evaluates the predicate inside the WF-012 read and MTX-019 cites it as the guard against CAP-019's silent-cross-version-comparison Failure Condition. The snapshots an admitted rebase creates are MTX-076's (AC-PRULE-025, S-13 Scoring), consumed here and not redefined. PRULE-032's decision dependencies are OD-002, OD-003 and OD-009, each ratified, plus OD-024; it withholds nothing. No predecessor behaviour exists."
    }
  }
}
